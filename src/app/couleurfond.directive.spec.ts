import { CouleurfondDirective } from './couleurfond.directive';

describe('CouleurfondDirective', () => {
  it('should create an instance', () => {
    const directive = new CouleurfondDirective();
    expect(directive).toBeTruthy();
  });
});
